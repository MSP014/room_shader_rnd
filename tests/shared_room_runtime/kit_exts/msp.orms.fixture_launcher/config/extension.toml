[package]
title = "ORMS Validation Fixture Launcher"
version = "0.1.0"
description = "Open the requested Room Map validation stage after Kit startup."
category = "Internal"
support_level = "Sample"

[dependencies]
"omni.usd" = {}
"omni.kit.viewport.ready" = {}

[[python.module]]
name = "orms_fixture_launcher"
