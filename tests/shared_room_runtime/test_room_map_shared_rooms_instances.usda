#usda 1.0

# Instanceable Omniverse composition used to compare Preserve and reversible
# Session de-instance policies without editing the referenced source component.
(
    customLayerData = {
        string "orms:fixtureOrigin" = "Referenced and instanceable Omniverse geometry"
    }
    defaultPrim = "World"
    metersPerUnit = 1
    upAxis = "Y"
)

def Xform "World"
{
    float3 primvars:ormsCameraPositionWorld = (8, 3, 5) (
        interpolation = "constant"
    )

    def DomeLight "RoomMapEnvironment"
    {
        float inputs:exposure = 0
        float inputs:intensity = 1000
        asset inputs:texture:file = @../../assets/_external/hdri/kloofendal_48d_partly_cloudy_puresky_4k.exr@
        token inputs:texture:format = "latlong"
        float xformOp:rotateX = -90
        uniform token[] xformOpOrder = ["xformOp:rotateX"]
    }

    def Xform "BuildingA" (
        instanceable = true
        prepend references = @test_room_map_shared_rooms_omniverse.usda@</World/Building>
    )
    {
        double3 xformOp:translate = (0, 0, 0)
        uniform token[] xformOpOrder = ["xformOp:translate"]
    }

    def Xform "BuildingB" (
        instanceable = true
        prepend references = @test_room_map_shared_rooms_omniverse.usda@</World/Building>
    )
    {
        double3 xformOp:translate = (0, 2, 0)
        uniform token[] xformOpOrder = ["xformOp:translate"]
    }
}
