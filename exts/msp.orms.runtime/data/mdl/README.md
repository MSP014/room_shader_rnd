# Packaged MDL Content

This directory owns the canonical ORMS MDL modules for both source-checkout
development and release bundles. Packaging copies the extension tree directly,
so the installed material is always built from these exact files.

The `diagnostics/` modules are retained renderer-investigation evidence and are
not registered as artist-facing ORMS materials.
