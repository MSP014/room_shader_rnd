# Packaged MDL Content

The release bundle places the canonical `room_map.mdl` and
`room_map_single.mdl` files here. During source-checkout development, the
extension resolves the canonical files from `src/mdl/` instead, so authored MDL
is not duplicated in version control.
